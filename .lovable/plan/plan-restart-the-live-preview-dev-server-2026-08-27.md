Plan: Restart the live preview dev server

1. Stop the current `vite` dev-server process.
2. Wait for the supervisor to respawn it and for `http://localhost:8080/` to respond.
3. Verify the preview loads the home page without a blank screen.
